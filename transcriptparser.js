
function parseText(rawText){
    console.log(rawText);
    //Get each variable for a student
    //
    // Parse the string text here.
    //
    let student = new Student(variables);
    return student;
}